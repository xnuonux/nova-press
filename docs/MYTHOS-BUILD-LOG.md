# nova press · the mythos · build log

> the perfection-loop tracker. the autonomous build reads this each fire, does the next increment, verifies it, commits, updates this log, and schedules the next fire. north star: `docs/07-mythos-vision.md`. architecture: `docs/08-editorial-system-architecture.md`. branch: `feat/mythos-editorial-system`.

## the loop contract (every fire honors this)

1. read this log. pick the next unchecked increment.
2. build it (additive, mirror the existing seams, lunari voice ... lowercase, no em-dashes, `...` for pauses).
3. verify: `pnpm typecheck` + `pnpm test` green; em-dash sweep on new files = 0; for UI, `playwright` e2e + a screenshot read back to confirm it renders.
4. commit on the branch (conventional, no em-dashes in the message). never main.
5. update this log (check the box, note what shipped). schedule the next fire.
6. if genuinely blocked on a decision only dom can make, stop and say so. otherwise keep going.

## status

- last completed: **phase 4.4** ... the codex grows itself + the partner reads the bible + the per-piece scan (the phase-4 follow-ups). the detect -> codex loop is closed, the sparring partner is grounded in the world, and the continuity rail stays fresh per-piece with no model. the DETERMINISTIC spine first (pure, unit-tested headless): `src/lib/continuity/flag-draft.ts` (`flagToCodexDraft` ... an "unintroduced" flag maps to a pre-filled draft, a name_drift maps to nothing), `src/lib/ai/codex-propose.ts` (`buildProposePrompt` windowed around the mention + `parseProposal` ... xray-shaped, pins the kind to the six, bounds + dash-strips the summary, degrades to {}). then the server + db: `codex-propose-analyze.ts` (`proposeEntity` ... reads the prose, degrades to {} with no model) + `proposeEntityDraftAction` (rate-limited + name-capped like every model boundary), `scanPiece` on `continuity.ts` (the DETERMINISTIC pass over ONE piece, replacing only that piece's open name_drift/unintroduced flags ... the whole-work scan's model + other-piece flags untouched, quiet until a bible exists), wired off the editor autosave (best-effort) + an on-demand `POST /api/ai/continuity/scan-piece`; the partner bible slot (`buildPartnerPrompt` + `PartnerInput.bible`) filled by `readBibleForWorkMentioned` in the command route (owner-scoped, degrades to no-bible). then the UI: an "add to codex" affordance on the rail's unintroduced flags -> a `nova:codex-prefill` document event -> the `CodexPanel` opens + pre-fills its add form (re-mounting on a nonce so a fresh proposal lands). the MAJOR fix this phase: the deterministic flag id now keys on the STABLE mention identity, not the message (the "unintroduced" message embeds a recurrence count that ticks up, which used to shift the id + re-raise a dismissed flag on the next autosave). verified: typecheck + 643 tests green (flag-draft / propose / window / partner-slot pins), em-dash sweep 0, a playwright e2e ... the per-piece scan route returns flags with no model; the partner riposte came back IN-WORLD ("let him speak only if the silence would say more", grounded in marik's never-speaks fact); the "add to codex" loop proposed a kind + an in-world summary for "Sable" ("a woman who watches from the reeds, followed by the dead") + saved it to the codex (db-confirmed) ... + a screenshot read back. review pass (4.4): a 6-lens adversarial workflow raised 16, confirmed 6, all fixed ... MAJOR: the flag-id-stability bug above (a dismissed unintroduced flag no longer re-raises when the count ticks up). MINORS: `proposeEntityDraftAction` now rate-limited + name-capped (it was the lone un-windowed model boundary), the command route's `getPieceById` wrapped so a transient read degrades to no-bible instead of 500ing the riposte, the codex prefill re-mounts on a nonce (a same-name re-proposal lands its enriched fields), and the propose prompt windows the prose around the mention (a name recurring past the head of a long work).
- prior: **phase 4.3** ... the codex + the continuity rail + retrieval-by-mention + series. **phase 4 (world bible + continuity + series) is complete.** the bible is now an editable surface, the prose reads against it in a rail, a beat pulls only the cast it names, and a work can sit in a series sharing one codex. the DETERMINISTIC spine first (pure, unit-tested headless): `src/lib/codex/validate.ts` (`normalizeEntity` ... the six kinds, trimmed + de-duped + bounded, the self-alias dropped BEFORE the breadth cap), `src/lib/continuity/retrieve.ts` (`mentionedEntityIds` ... an exact / name-token / pg_trgm near-miss match of a beat's capitalized words against the bible names + aliases; co-named entities both retrieved, a stopword-named entity still found, a fumbled spelling still grounds the right entity), `src/lib/works/series.ts` (`descendantIds` / `wouldCycle` / `parentCandidateIds` ... the series-graph cycle guard). then the db + the routes: `src/lib/db/codex.ts` (create/edit/delete an entity + its alias/fact children, scoped to the EFFECTIVE bible work, RLS owner; a child-write failure is surfaced not swallowed + a create rolls the orphan back so it's all-or-nothing; an update checks its child deletes so a stale alias never collides with the unique index + silently eats the edit), `triageFlag` + `isContinuityStatus` on `continuity.ts`, `src/lib/db/series.ts` (`setWorkParent` ... attach/detach + carry the whole branch to the shared bible, the branch-defining parent write committed LAST so an interrupted run never strands a sub-branch), `composeBibleForMentions` on `bible-compose.ts` + `readBibleForWorkMentioned` on `bible.ts` narrowing the author prompt's bible slot to the beat's cast, the scan route returning the work's open flags. then the UI: the `CodexPanel` + `ContinuityRail` islands (calm disclosure panels, lunari + the golden accent, the pass-panel triage with optimistic-rollback + upsert-on-reconcile) + a series form, all mounted on the work page. verified: typecheck + 627 tests green (the validate / retrieve / series / compose-for-mention pins), em-dash sweep 0, a playwright e2e ... add a codex entry "Lethe" -> it renders; run a continuity scan -> 2 flags surface; accept a flag -> it greys + shows undo; the series form + the rail render ... + a screenshot read back. review pass (4.3): a 6-lens adversarial workflow raised 20, confirmed 11, all fixed ... MAJORS: `updateCodexEntity` now CHECKS its child deletes + surfaces a child-write failure (a swallowed delete collided with the alias unique index + silently lost the edit; a swallowed insert wiped the children under a false ok), the codex panel's `fromLines` splits on NEWLINE ONLY (a comma split shredded any fact/alias holding a comma on a benign re-save), and the rail's optimistic triage now ROLLS BACK on a server refusal (it left the rail + the open-count diverged from the server). MINORS/NITS: the codex + series server actions uuid-guard before `getWorkById` (a malformed id was a raw 500), `createCodexEntity` rolls back an orphan on a child-write failure, retrieval keeps BOTH co-named entities + a stopword-named one (no first-write-wins drop), `setWorkParent` commits the branch-defining write last (no half-migrated branch), the triage reconcile upserts (a concurrent scan can't drop the row), the self-alias is filtered before the cap, and the stale codex note no longer bleeds across flows.
- prior: **phase 4.2** ... the continuity LAYER (the model + the scan over the v0_10_0 schema). nova now reads a work's prose AGAINST its bible. the DETERMINISTIC spine first (pure, exact, unit-tested like the editorial lenses): `src/lib/continuity/` ... `trigram.ts` (a pg_trgm-shaped jaccard similarity, so the js drift agrees with the bible's gin trigram index), `detect.ts` (NAME-DRIFT ... a mid-sentence proper noun trigram-close to a bible name but not it, "marrik" -> "marik"; UNINTRODUCED ... a recurring proper noun not in the bible; the MID-SENTENCE requirement + a stoplist + a possessive strip are the false-positive discipline), `hash.ts` (the content fingerprint, an FNV clone of the dispatch edition hash). then the xray-shaped MODEL layer (`src/lib/ai/continuity-model.ts` + `continuity-analyze.ts`): a pure prompt + defensive parse for CONTRADICTION + TIMELINE, degrading to [] with no model, the parse pinning kind + mapping the piece index to a real id + stripping em-dashes. then the RESUMABLE scan (`src/lib/db/continuity.ts` `scanWork`): hash the prose AND the bible, skip-if a 'complete' np_continuity_scans row exists for (work_id, body_hash), else open in_progress, run both passes, write np_continuity_flags (deterministic ids ... idempotent under a race + a dismissed concern never re-raises), walk to complete (only if the model pass ran; a model FAILURE stays in_progress to retry). `POST /api/ai/continuity/scan` (auth + tight rate window + owner-scoped + uuid-guarded). verified: typecheck + 594 tests green (the trigram / detect / hash / parse pins), em-dash sweep 0, and a route e2e ... seeded a piece misspelling the bible's "marik" as "Marrik" (+ a recurring "Sable"); the scan raised a name_drift flag (entity-linked) + an unintroduced flag, walked the scan to complete, and a re-scan of unchanged content was SKIPPED. review pass (4.2): a 4-lens adversarial workflow raised 16, confirmed 9, all fixed ... MAJORS: a possessive ("Marik's") no longer false-flags the canonical name, calendar names no longer over-fire, a model FAILURE no longer caches as a false 'complete' (it stays in_progress to retry), the staleness hash now folds in the bible (editing the codex re-scans), `readBibleForWork` now scopes to the work (it was reading every work's bible), and the model message strips em-dashes (the HARD invariant on the one model-free-text path). MINORS: a tag-first dialogue opener no longer reads as a name, the flag writes are idempotent (deterministic ids + guarded open), and the scan_token preview-gate is honestly noted as deferred to 4.3.
- prior: **phase 4.1** ... the world bible + continuity SCHEMA (the data layer; the model-driven scan is 4.2). two migrations, APPLIED + verified live: `v0_9_0_np_bible.sql` is the codex ... `np_bible_entities` (the nouns: character|place|object|faction|event|lore, work-scoped, plain-uuid `work_id`), `np_bible_aliases` + `np_bible_facts` (the one blessed intra-nova FK cascade `entity_id -> np_bible_entities ON DELETE CASCADE`, foreshadowed in v0_7_0 ... a dangling alias/fact would corrupt continuity itself), with `pg_trgm` gin trigram indexes on the lowered name + alias for the 4.2 fuzzy match. `v0_10_0_np_continuity.sql` is the flags + scan watermark ... `np_continuity_flags` (work/piece/entity plain-uuid, kind CHECK, a descriptive message + a status open|accepted|dismissed mirroring the editorial Finding triage so the continuity rail reuses the pass-panel) + `np_continuity_scans` (resumable, two-phase, cloning `np_newsletter_dispatch`: UNIQUE(work_id, body_hash) skips a re-scan of unchanged content, status preview->in_progress->complete, a `scan_token` gating the commit). every table RLS four-policy owner-only + the shared `tg_set_updated_at` trigger; cross-product FK only to `auth.users`; the pg_dump exit ramp stays clean. THE PAYOFF: the RESERVED bible slot in `author-prompt.ts` is finally filled ... a pure `src/lib/db/bible-compose.ts` (`composeBible` ... a bounded "name (kind) ... summary [also: aliases]" + dashed facts, per-field + breadth + total-length capped) + a server `src/lib/db/bible.ts` (`readBibleForWork` ... resolves `bible_work_id` so a series volume shares its codex, reads entities + embedded aliases/facts most-recent-first, NEVER throws) threaded into the author route. verified: typecheck + 560 tests green (the composeBible pin), em-dash sweep 0, the migration post-conditions confirmed live (5 tables, RLS on, 4 policies + trigger each, the trigram gin + unique indexes, the FK cascades), and an end-to-end e2e ... seeded a bible (marik, the mute ferryman of the seventh gate, ferries only the dead), ran `/expand`, and nova's beat came back IN-WORLD ("old marik stood at the edge of the water ... didn't acknowledge the living weight ... the seventh gate groaned open"). review pass (4.1): a 4-lens adversarial workflow raised 7, confirmed 2 (both minor), both fixed ... composeBible now hard-caps each field (so one fat entry can't blow the slot, matching the route's document/context slices), and the read orders most-recent-first (so a large bible keeps the freshly-introduced nouns, not the oldest).
- prior: **phase 3.2** ... poetry (zero migration). **phase 3 (the ai author + poetry) is complete.** verse is a first-class form: it rides `np_pieces.body` as a flat run of `verse_line` blocks (mirroring the ul_li/ol_li list model), grouped into stanzas + poems on read. the pure spine is `src/lib/editorial/poetry.ts` (`countSyllables` vowel-group heuristic, `rhymeKey`/`rhymes` end-rhyme key, `groupVerse` poems->stanzas->lines with empty-line / blank-paragraph stanza breaks, `modal` + `establishedMeter`). three deterministic, SELF-GATING lenses (silent on prose) read a poem against ITSELF, descriptive mirrors never verdicts: `scansion` (a line off the poem's own established measure ... free verse stays quiet, a word-less "..." line is never judged), `rhyme` (a couplet that doesn't chime, only on a poem clearly built on couplets ... a >=3 substantive-chime floor stops coincidental suffix rhymes fabricating a scheme), `form-shape` (a stanza off the dominant shape ... a deliberate closing tag like a sonnet's couplet / a ballade's envoi is left alone). the editor gains the flat `verse_line` plate block (`verse-blocks.tsx`, a slash `verse` item, enter-on-empty-exits like a list); the reading view gains a `verse` group in `groupBodyBlocks` + a `.np-verse`/`.np-verse-line` renderer (lines hold their breaks, a golden left rule). the repair half is the `coin` author task (a fourth `AUTHOR_CONFIG` task ... `AuthorConfig.oneBeat` became `stop: paragraph|line|none`; coin stops at one line via `lineCut`, runs hot, fits the poem's measure + rhyme from the document, weaves in as a `verse_line`), wired through `author.ts` / `author-prompt.ts` / `author-beat.tsx` / the route / a slash `/coin` item. verified: typecheck + 550 tests green (poetry + lens + lineCut pins), em-dash sweep 0, a playwright e2e (the published reading view renders the poem as verse; an editorial pass surfaces the scansion flag on the meter-breaking line; `/coin` streams a line in voice that weaves into the poem, 4->5 lines) + screenshots. review pass (3.2): a 4-lens adversarial workflow raised 11, confirmed 7, all fixed ... MAJOR: coin's `lineCut` was blind to a leading newline (it could fold a two-line coin into one over-measure line); now it skips leading whitespace like `paragraphCut`. MINORS: scansion no longer judges a 0-syllable "..." line, the rhyme gate no longer fabricates a couplet scheme from coincidental suffix rhymes (substantive-key chime + a >=3 floor), and form-shape leaves a sonnet's closing couplet / an envoi / a tornada alone. NIT: the coin prompt's uppercase "FIT" lowercased.
- prior: **phase 3.1** ... the ai author. the generative counterpart to the editorial brain: nova drafts a beat (a paragraph) or an outline IN the writer's resolved voice, on command from the editor slash menu, never running past what was asked. `AUTHOR_CONFIG`/`isAuthorTask` in `provider.ts` (three tasks: `expand` / `draft-beat` / `outline`, the beat tasks stop at the first paragraph break, the outline is a skeleton); `src/lib/ai/prompts/author-prompt.ts` (`buildAuthorPrompt` ... the voice-mirror block order, the task instruction, and a RESERVED bible slot for phase 4); `src/lib/ai/author.ts` (`streamAuthor` ... dash-safe per chunk, `paragraphCut` ends a beat, ignores a stray leading blank line); `src/lib/ai/author-format.ts` (the pure `auditedLines` ... a beat is one paragraph, an outline is voice-kept PER LINE so every line stays lowercase); `POST /api/ai/author` (auth + per-user rate window, OWNER-SCOPED `getPieceById` with a uuid shape guard, voice baseline, streams). the UI is one calm island, `AuthorBeat`, mounted beside the ghost: the slash menu (`expand` / `draft this beat` / `outline`) fires a `nova:author` event, the island streams a greyed beat anchored to the block (flipping above when there's no room below), and tab weaves it in as paragraph(s) after the note (non-destructive), escape/edit/scroll wave it off ... and it raises `nova:author-active`/`-idle` so the ghost whisper stands down while it's on screen (the two never both claim tab). verified: typecheck + 511 tests green (the pure provider/prompt/format/`paragraphCut` pins), em-dash sweep 0, a playwright e2e (slash `/expand` -> nova streams a beat in voice -> tab -> a new paragraph woven in, blocks 2->3) + a screenshot read back. review pass (3.1): a 4-lens adversarial workflow raised 10, confirmed 8, all fixed ... MAJORS: the beat now dismisses on a real edit once it's DONE (a stale `blockIndex`/anchor no longer misplaces the insert; armed only when done so the summoning slash-delete echo can't kill it mid-stream), the outline accept now lowercases EVERY line (the keeper only lowercased the first paragraph), and the card flips above + guards its own overflow scroll so a low/tall beat stays reachable; MINORS/NITS: tab is swallowed mid-stream (no stray `\t` in the note), a leading blank line no longer closes an empty beat, a non-uuid pieceId is a clean 400 (not a raw 500 from outside the try), the error card shows an esc cue, and the insert path clamps `blockIndex` to the doc length.
- prior: **phase 2.4** ... the editorial UI + the routes that feed it. **phase 2 (the editorial brain) is complete.** the craft axis is now a thing a writer can see and work: `POST /api/ai/editor/pass` (auth + a per-user rate window, RLS-owned `getPieceById`, runs the stage's deterministic lenses over the SAVED body against the writer's own voice baseline, `upsertPass` watermarked to `last_edited_at`), the server-authoritative `editorial-actions.ts` (`triageFindingAction` accept/dismiss/reopen, `setStageAction` THROUGH `setStage`'s gate), and the calm `EditorialPanel` island ... a slim ladder stepper (the current stage lit in `--nova-accent`, a staleness dot, the advance button gated with the gate's own reason) over a disclosure pass panel (per-finding accept/dismiss/undo, the advance opening only once every FLAG is triaged). it never touches the editor value; `plate-shell` dispatches `nova:piece-edited` on a real edit so the panel stales itself, and a `nova:flush-save` bridge lands any pending autosave before a pass so the pass reads the current body. verified: typecheck + 484 tests green, em-dash sweep 0, and a playwright e2e (run a pass -> the broken-link FLAG -> the gate shuts -> dismiss -> the gate opens -> advance line->copy -> retreat RESTORES the stored pass) + a screenshot read back (the panel sits in the editor column, clear of the partner rail + the chrome). review pass (2.4): an adversarial workflow raised 11, confirmed 10, all fixed ... the headline was a MAJOR layout overlap (a viewport-centered panel painted over + swallowed clicks on the 320px partner rail; now centered on the editor column at `lg` + dropped below the chrome band), plus the retreat-restore (a step back no longer blanks the prior review), the autosave-flush honesty (a pass no longer optimistically clears staleness over unsaved edits), the advance a11y (`aria-disabled` + the reason routed to the visible note, so keyboard/AT reaches it), the triage status whitelist + the `setStage` not-found guard, the gate-hint unified through `evaluateTransition`, and `aria-expanded`/`aria-current` + silent-failure notes.
- prior: **phase 2.3** ... the editorial state machine + the db layer. the pure advance-gate `src/lib/editorial/state-machine.ts` (`evaluateTransition` over the ladder + the Finding triage): a retreat / no-op is always free, a 2+ step skip never, and an advance opens ONLY when the current stage's pass is current (not stale) and every FLAG finding is triaged (notes never gate); the result is descriptive (a reason in voice, not a bare boolean). `src/lib/db/editorial.ts` is the `np_editorial_passes` layer (authed client, RLS owner-only, the typed-client cast + `rowToPass` mapper): `upsertPass` (one row per piece+stage, the `piece_id,stage` conflict), `listPassesForPiece` / `getPass` / `getPiecePasses` (annotates read-time staleness via the piece's `last_edited_at`), `getPieceStage`, `setStage` (reads the piece + the gating pass, runs the gate, writes `editorial_stage` + appends `metadata.editorial.history` trimmed to 20, ONLY when allowed), `deriveWorkStage` (min over the work's pieces). 16 new tests (the gate exhaustively + the mapper + the db shapes + setStage advance/block/retreat). review pass (2.3): 8 raised, 3 nits fixed (a singular subject-verb slip in the flags reason, and `asFindings` now drops corrupt elements like `asStrings`).
- prior: **phase 2.2** ... the Finding/Lens layer. the unifying abstraction `type Lens = (LensInput) => Finding[]` lives in `src/lib/editorial/lenses/`. the deterministic spine (exact, free, unit-tested): `mechanical` (double spaces, a straight quote the curler missed, a heading-level skip, a broken/unsafe link ... read over PROSE only, so a straight quote inside inline `code` is never a false slip), `readability` (a sentence past the writer's OWN sentence-length baseline, never a generic grade), `voice-drift` (the whole piece's avg sentence length diverging from the resolved voice). the one model lens, `structure`, is `xray.ts` promoted: its threads become descriptive notes, degrading to [] with no x-ray. a `registry` maps lenses to the craft stages (mechanical at line/copy/proof, structure at developmental, ...) + a total `runStage` (a throwing lens is skipped). every Finding is descriptive-first, lowercase, scoped, severity note|flag ... a mirror, never a verdict. `voice-stats.ts` now exports `countWords` + `sentenceWordCounts` (the ONE definition of "a sentence", so the baseline and the lenses measure identically). 26 new tests. review pass (2.2): 10 raised, 8 fixed ... the headline was a descriptive-rule breach (the double-space note said "a single space sets cleaner", a verdict + advice; now a pure mirror), plus the inline-code false positive, the sentenceWordCounts single-source, and three cosmetics.
- prior: **phase 2.1** ... the editorial brain's schema + the pure state machine. `v0_8_0_np_editorial.sql` (APPLIED to the live substrate) adds the CRAFT AXIS: `np_pieces.editorial_stage` (drafting -> developmental -> line -> copy -> proof -> typeset -> exported, defaulted, CHECK-bounded) + `np_editorial_passes` (one pass per piece+stage: a `findings` jsonb, `lens_keys`, the `source_edited_at` staleness watermark, `pass_metadata`; plain-uuid `piece_id`, RLS four-policy, the shared `tg_set_updated_at` trigger, unique `(piece_id, stage)`). regenerated `src/types/supabase.ts`. the long-standing `PieceStatus` drift is fixed ... `src/types/index.ts` no longer lists `editing`/`unpublished` (which the db CHECK always rejected; "editing" was never a publication state ... it's the craft axis). new domain types `src/types/editorial.ts` (EditorialStage, Finding, EditorialPass) + the pure `src/lib/editorial/stages.ts` (the ladder, forward-gated single-step advance + backward-free retreat, `workStage` = min over pieces, `isPassStale` read-time watermark) with a 9-test pin. verified: migration post-conditions confirmed live (column + table + RLS on + 4 policies + 3 indexes + 1 trigger); typecheck + 441 tests green. review pass (2.1): 4 raised, 3 nits fixed (workStage returns the canonical floor stage, `pass_metadata` -> `passMetadata`, the table comment lowercased in both the file + the live db).
- prior: **phase 1.4** ... docx + epub export, docx import. **phase 1 (prose works) is complete.** Slate JSON stays the one source of truth: `src/lib/io/docx.ts` (slateToDocx piece + sectionsToDocx work + docxToSlate via mammoth->markdown->the existing hub), `src/lib/io/html.ts` (a pure escaped Slate->html serializer), `src/lib/io/epub.ts` (epubChaptersFromSections + workToEpub via @lesjoursfr/html-to-epub into a temp dir). three binary routes mirror /api/export + /api/import (auth + rate-limit + owner-scoped + size caps): `POST /api/export/docx` (piece or work), `POST /api/export/epub` (work), `POST /api/import/docx` (a .docx -> a new piece). `next.config` keeps the epub lib external. verified end to end AUTHED with a real work: docx-piece + docx-work (200, word mime, PK zip), epub (200, application/epub+zip), docx import round-trips to a new piece, anon export 401; unit pins on every converter incl. real docx + epub byte renders. review pass (1.4): 8 raised, all 8 fixed ... the major one was the epub lib writing its working dir inside node_modules (leaks on error + breaks on a read-only prod fs); now `tempDir` keeps it under our cleaned-up temp dir. also: per-list ol numbering, work-docx heading shift, an import content-length precheck, the platform no longer stamped as the book's author, a reworded comment, and a hoisted `fileSlug`.
- prior: **phase 1.3** ... work-level reading at `/w/[slug]`. a published Work renders as one magazine-grade reading experience: a contents TOC from the node tree + each leaf's prose in reading order (pure `flattenForReading` → depth-first sections, pinned). `publishWork` mirrors `publishPiece` (owner-scoped, the `np_works_published_slug_uniq` partial index, 23505 retry, empty-work guard); `getPublishedWorkBySlug` is a service-role read gated on the work's `status=published` + `visibility in (unlisted,public)` ... a draft/private work can never surface. the work page gains a publish button / "published ... read it" link by state. verified end to end: an anon (no-cookie) context read a published work (200, title + TOC + prose), an unknown slug 404s, and after flipping the work to draft an anon read returned 404 (the gate holds). review pass (1.3, security-first): 9 raised, 5 fixed ... the leaf-body read is now scoped to `work_id` (no cross-owner pointer), the node read selects explicit columns (no `user_id` off the service-role client), leaf titles are real headings (outline a11y), empty leaves + hollow containers are pruned from the published view, and the dead `first:mt-0` part-spacing is gated on the section index.
- prior: **phase 1.2** ... work-scoped voice extraction. a new `listPieceTextsForWork(client, workId, limit)` reads ONLY a work's prose (np_pieces by `work_id`, non-archived, recent first, empties dropped); `POST /api/voice/extract` now takes an optional `{ workId }` and branches the source (a missing / malformed / non-uuid body falls back to the library-wide read, so the bare no-body POST is unchanged); `VoiceTrainer` gains optional `workId` + `label` props and mounts on `/work/[id]` as "train nova on this work". the result still lands in the one voice_profiles row (last-writer-wins). verified end to end + db: a fresh writer with 2 standalone library pieces AND 2 work pieces, trained work-scoped, POSTed `{ workId }`, the route returned `{scope:"work", samples:1}`, and `voice_profiles.writing_samples_count` persisted as 1 ... the standalone pieces were excluded, proving the scope. review pass (1.2): a 3-lens adversarial workflow raised 6, confirmed 2 nits, both fixed (a non-uuid workId now falls back to the library read instead of 502-ing on the uuid cast; `router.refresh()` is gated to the library case so a work-scoped train skips a pointless work-tree re-fetch).
- prior: **phase 1.1** ... the corkboard + word targets + the subtree word-count rollup. each binder row now carries a card: a one-line synopsis (the `np_nodes.synopsis` column), a live subtree word count, and an optional word target (rides `node_metadata.wordTarget`) with a quiet golden meter. the rollup is wired into the editor's autosave: when a Work-bound leaf saves, `recomputeWorkWordCounts` rolls the leaf's words up its ancestor chain + the work total (pure `rollupWordCounts` does the math, pinned by 3 tests). a standalone library piece has no `workId` and skips it, so `/editor` carries zero extra cost. verified end to end with a REAL playwright run + db read: typed 16 words into a leaf, autosave rolled them up to chapter 1 / act one / the work header (16 words); set a card on act one and it shows "16 / 500" with the synopsis line; db confirms `word_count=16` on the chain + `node_metadata {"wordTarget":500}`.
- current fire: **phase 5.1** ... conlang (the first reference form). a constrained coiner that invents words to a work's phonology + the lexicon as record-leaves: a phonology registry in `work.settings` (the allowed sounds + syllable shapes), a `coin-word` author task that generates a candidate word obeying those constraints (deterministic phonotactics check first, the model only proposes within the rails), a lexicon record-leaf render (word / gloss / part-of-speech), and a grammar reference page. deterministic spine first (the phonotactics validator + the lexicon record shape, pure + unit-tested), then the db / form registry, then the author task + the UI. + playwright + a screenshot. (phase 4 ... world bible + continuity + series ... is complete.)
- the suite: 643 tests green.
- applied migrations on the live substrate: `v0_7_0` (works/nodes/links), `v0_8_0` (editorial stage + passes), `v0_9_0` (world bible: entities/aliases/facts), `v0_10_0` (continuity: flags + resumable scans).
- review pass (1.1): an adversarial review workflow (4 lenses, every finding skeptic-verified) caught 9 real issues; all fixed + re-verified. the headline one was a blocker ... the corkboard synopsis was trapped inside a closed `<details>`, so it never rendered at rest; it now lives outside the fold (playwright confirms it's visible with zero details open). also: the card toggle is keyboard/touch reachable (opacity-40 + focus-visible), `setCardAction` merges into `node_metadata` instead of clobbering it, the work word_count write is drift-gated, recompute reads a narrow column set (no jsonb per autosave), the save-card button uses `--lunari-bg-deep`, the input focus border renders golden (computed `rgb(201,168,76)`), and a standalone piece's autosave folds `work_id` into the existing write so it pays zero extra queries.
- known follow-up (cheap): a seeded skeleton leaf (e.g. the novel's "scene 1") is a placeholder label with no piece yet, so it isn't openable until you "+ page" a real leaf. lazy-mint a piece on first click of a skeleton leaf (a small open-or-create action) so the seeded scaffold is writable straight away.

## the ladder

### phase 0 ... the keystone

- [x] 0.1 `v0_7_0_np_works.sql` + `src/types/works.ts` + `src/lib/forms/` (registry + constraints + test) + the vision/architecture docs.
- [x] 0.2 applied `v0_7_0` to the live substrate (3 tables + 12 policies + 3 np_pieces columns, RLS on) + regenerated `src/types/supabase.ts`; built the db layer `src/lib/db/works.ts` + `nodes.ts` + `links.ts` (`createWork` + skeleton seed, `createNode` / `createLeafNode` / `moveNode` / `deleteNodeSubtree`, `getWorkTree`, `promotePieceToWork`, `createLink` / `backlinks` / `resolveLink`) + the pure `src/lib/works/tree.ts` + `map.ts` + a 9-test pin.
- [x] 0.3 the import/export keystone: installed `@platejs/markdown`; `src/lib/io/markdown.ts` (`markdownToSlate` / `slateToMarkdown` via a headless `createSlateEditor` + basic-nodes + link + MarkdownPlugin) + a 5-test round-trip pin; `POST /api/import` (md -> Slate -> a new piece, RLS-owned) + `POST /api/export` (pieceId -> Slate -> md). Slate JSON is the one source of truth, proven end to end.
- [x] 0.4 the binder at `/work/[id]`: server component rendering the tree (`getWorkTree`) as an outliner, a leaf links `/editor/[pieceId]` (the existing plate-shell verbatim), add-page (`createLeafNode`) + up/down reorder (`moveNode` + `midpointPosition`) via form-actions; `src/components/binder/binder.tsx` + `src/app/(authed)/work/[id]/{page.tsx,actions.ts}`. `/library` gains a "new work" form picker (`newWorkAction`, 8 forms) + the `promotePieceToWork` action (`work-actions.ts`); the existing /library + /editor + /p/[slug] are untouched. screenshot verified authed-full.
- ship gate (met): a writer creates a Work, sees the structure in the binder, opens a leaf in the editor, imports a markdown file (/api/import), exports it back (/api/export). every existing flow still works (suite 404 green). NOTE: the per-card "promote" button is a quick follow-up (the action is built); the binder uses up/down reorder (drag is a later polish).

### phase 1 ... prose works

- [x] 1.1 the corkboard + word targets + the subtree rollup: per-row card (synopsis + a live subtree count + a word target with a golden meter); pure `rollupWordCounts` (3-test pin) + `recomputeWorkWordCounts` (writes only the drifted node rows + the work total) wired into the editor save action (gated on a `workId`, so standalone pieces are untouched); `setCardAction` writes the synopsis + `node_metadata.wordTarget`; `updateNode` learns `nodeMetadata`. screenshot + db verified: 16 words roll leaf -> chapter -> act -> work; act one shows "16 / 500".
- [x] 1.2 work-scoped voice extraction: `listPieceTextsForWork` (np_pieces by `work_id`) + `POST /api/voice/extract` optional `{ workId }` branch (uuid-shape-checked, falls back to the library read) + `VoiceTrainer` `workId`/`label` props mounted on `/work/[id]`. e2e + db verified: a writer with 2 standalone + 2 work pieces trains work-scoped -> `{scope:"work", samples:1}`, `voice_profiles.writing_samples_count=1`. review: 6 raised, 2 nits fixed.
- [x] 1.3 work-level reading at `/w/[slug]`: pure `flattenForReading` + `pruneEmptyReading` (pinned) → a contents TOC + per-leaf `PieceBody` in reading order; `publishWork` (owner-scoped, slug-collision retry, empty guard) + `getPublishedWorkBySlug` (service-role, gated `published` + shareable, work-scoped node/piece reads, no owner columns); the work page publish/read affordances. e2e + gate verified: anon reads a published work (200), unknown slug + draft work both 404. review: 9 raised, 5 fixed (security: scoped piece read + explicit node columns; a11y leaf headings; empty-section prune; spacing).
- [x] 1.4 docx + epub export, docx import: `io/docx.ts` (slateToDocx + sectionsToDocx + docxToSlate), `io/html.ts` (escaped Slate->html), `io/epub.ts` (chapters + workToEpub); `POST /api/export/docx` (piece|work) + `/api/export/epub` (work) + `/api/import/docx` (-> piece), all auth + rate-limit + owner-scoped; `next.config` keeps the epub lib external. e2e authed: real docx/epub bytes (PK + the right mimes), docx import round-trips, anon 401. review: 8 raised, all 8 fixed (incl. the epub temp dir -> `tempDir` under os.tmpdir).

### phase 2 ... the editorial brain

- [x] 2.1 `v0_8_0_np_editorial.sql` (APPLIED): `np_pieces.editorial_stage` (the 7-stage craft axis) + `np_editorial_passes` (findings + `source_edited_at` watermark, RLS owner-only, unique per piece+stage); regenerated types; fixed the `PieceStatus` drift; `src/types/editorial.ts` + the pure `src/lib/editorial/stages.ts` (forward-gate / backward-free / workStage-min / staleness) with a 9-test pin. review: 4 raised, 3 nits fixed.
- [x] 2.2 the Finding/Lens layer (`src/lib/editorial/lenses/`): `type Lens = (LensInput) => Finding[]`; deterministic mechanical + readability-vs-own-voice + voice-drift lenses, `xray.ts` promoted as the `structure` lens, a stage-mapped registry + total `runStage`; `voice-stats.ts` exports the one `sentenceWordCounts` definition. descriptive-only (a mirror, never a verdict). 26-test pin; review 10 raised, 8 fixed.
- [x] 2.3 `src/lib/editorial/state-machine.ts` (pure `evaluateTransition`: advance gated on a current + fully-triaged pass, retreat free, skip blocked) + `src/lib/db/editorial.ts` (`rowToPass`, `upsertPass` on the piece+stage conflict, `getPiecePasses` + read-time staleness, `setStage` through the gate + the trimmed history, `deriveWorkStage` min). 16-test pin; review 8 raised, 3 nits fixed.
- [x] 2.4 the editorial UI + the routes that feed it: `POST /api/ai/editor/pass` (stage lenses over the saved body + voice baseline, watermarked `upsertPass`), `editorial-actions.ts` (`triageFindingAction` + `setStageAction` through the gate, status whitelisted + not-found guarded), the `EditorialPanel` island (a ladder stepper lit in `--nova-accent` + a staleness dot + a gate-reasoned advance, over a disclosure pass panel with per-finding triage), `plate-shell` dispatching `nova:piece-edited` + a `nova:flush-save` autosave bridge so a pass reads the current body. e2e: run pass -> FLAG -> gate shut -> dismiss -> gate opens -> advance line->copy -> retreat restores the stored pass; + a screenshot. review: 11 raised, 10 fixed (the major panel-overlap, retreat-restore, flush honesty, advance a11y, the rest a11y + validation nits). **phase 2 complete.**

### phase 3 ... the ai author + poetry

- [x] 3.1 the ai author: `AUTHOR_CONFIG`/`isAuthorTask` (`expand`/`draft-beat`/`outline`) + `buildAuthorPrompt` (voice-anchored, RESERVED bible slot) + `streamAuthor` (dash-safe, `paragraphCut`) + pure `auditedLines` (outline lowercased per line) + `POST /api/ai/author` (owner-scoped, uuid-guarded, streams) + the `AuthorBeat` slash-menu island (greyed beat preview, tab-to-weave-in, ghost stands down via `nova:author-active`/`-idle`). 27 new tests; review 10 raised, 8 fixed (edit-dismiss when done, per-line lowercase, viewport flip + overflow guard, tab-swallow, leading-blank guard, uuid 400, esc cue, blockIndex clamp). e2e: slash `/expand` -> streamed beat in voice -> tab -> woven in (2->3 blocks) + screenshot.
- [x] 3.2 poetry (zero migration): the flat `verse_line` plate block (`verse-blocks.tsx` + a slash `verse` item + enter-on-empty-exits) + the `groupBodyBlocks` verse group + the `.np-verse`/`.np-verse-line` reading renderer; the pure `poetry.ts` (syllables / rhyme key / `groupVerse` / `establishedMeter`); the self-gating `scansion` / `rhyme` / `form-shape` lenses (descriptive mirrors, quiet on prose + free verse, leaving deliberate forms alone); the `coin` author task (a 4th `AUTHOR_CONFIG` task ... `oneBeat` -> `stop`, `lineCut`, fits the poem's measure, weaves in a `verse_line`) + a slash `/coin`. 39 new tests; review 11 raised, 7 fixed (the major coin leading-newline `lineCut`, scansion 0-syllable skip, rhyme substantive-chime floor, form-shape closing-tag, the FIT nit). e2e: reading renders the poem as verse -> a pass flags the meter-breaking line -> `/coin` streams + weaves a line in voice (4->5) + screenshots. **phase 3 complete.**

### phase 4 ... world bible + continuity + series

- [x] 4.1 the bible + continuity SCHEMA: `v0_9_0_np_bible.sql` (entities/aliases/facts ... the blessed FK-cascade knowledge graph + `pg_trgm` trigram indexes) + `v0_10_0_np_continuity.sql` (flags mirroring the Finding triage + the resumable two-phase `np_continuity_scans`), APPLIED + post-conditions verified live. the RESERVED bible slot is filled: pure `bible-compose.ts` (`composeBible`, bounded) + server `bible.ts` (`readBibleForWork`, resolves `bible_work_id`, never throws) threaded into the author route. 10 compose tests; review 7 raised, 2 fixed (per-field cap + most-recent-first order). e2e: a seeded bible grounds `/expand` in-world (marik / the seventh gate surface in the beat).
- [x] 4.2 the continuity LAYER: the deterministic pass (`src/lib/continuity/` ... pg_trgm-shaped `trigram.ts`, `detect.ts` name-drift + unintroduced with the mid-sentence + stoplist + possessive precision guards, `hash.ts` the FNV content fingerprint) + the xray-shaped model layer (`continuity-model.ts` + `-analyze.ts`, contradiction/timeline, degrades to [], em-dash-stripped parse) + the resumable scan (`src/lib/db/continuity.ts` `scanWork` ... hash prose+bible, skip-if-unchanged, in_progress->complete, deterministic-id idempotent flags, model-failure stays in_progress) + `POST /api/ai/continuity/scan`. 35 new tests; review 16 raised, 9 fixed (possessive + calendar + dialogue-opener precision, model-failure-not-complete, bible-in-hash, work-scoped bible read, em-dash strip, idempotent flag writes). e2e: a seeded "Marrik" misspelling -> a name_drift flag (entity-linked) + an unintroduced "Sable" flag -> scan complete -> re-scan SKIPPED. the per-piece incremental autosave-triggered scan is a noted 4.3 follow-up.
- [x] 4.3 the codex + continuity rail + flags inbox; retrieval-by-mention into the author prompt's bible slot; series as Work-of-Works. the pure spine (`codex/validate.ts` `normalizeEntity`, `continuity/retrieve.ts` `mentionedEntityIds`, `works/series.ts` cycle guard) + the db (`db/codex.ts` create/edit/delete with child-write honesty + atomic create rollback, `triageFlag`, `db/series.ts` `setWorkParent` branch-last) + `composeBibleForMentions` / `readBibleForWorkMentioned` narrowing the author slot + the `CodexPanel` / `ContinuityRail` islands + the series form on the work page. 33 new tests; review 20 raised, 11 fixed (the non-atomic update-children data loss, the comma-split round-trip shred, the un-rolled-back optimistic triage; + the uuid guards, co-named/stopword retrieval, branch-last series write, upsert reconcile, self-alias cap, note bleed). e2e: add "Lethe" to the codex -> renders; scan -> 2 flags; accept -> greys; series form + rail render + a screenshot. **phase 4 complete.**
- [x] 4.4 the codex grows itself + the partner reads the bible + the per-piece scan. the pure spine (`continuity/flag-draft.ts` `flagToCodexDraft`, `ai/codex-propose.ts` windowed prompt + defensive parse) + the server/db (`codex-propose-analyze.ts` `proposeEntity` degrading to {}, `proposeEntityDraftAction` rate-limited + name-capped, `scanPiece` ... the deterministic per-piece pass replacing only that piece's name_drift/unintroduced flags, wired off autosave + `POST /api/ai/continuity/scan-piece`) + the partner bible slot (`buildPartnerPrompt` + the command route's `readBibleForWorkMentioned`) + the "add to codex" rail affordance -> `nova:codex-prefill` -> the `CodexPanel` pre-fill. 15 new tests; review 16 raised, 6 fixed (MAJOR: the flag id now keys on the stable mention not the count-bearing message, so a dismissed unintroduced flag never re-raises; + propose rate-limit/name-cap, command-route getPieceById wrapped, prefill nonce remount, propose prose windowed on the mention). e2e: the per-piece scan route returns flags no-model; the partner riposte came back in-world (grounded in marik's never-speaks fact); the add-to-codex loop proposed a kind + in-world summary for "Sable" + saved it (db-confirmed) + a screenshot. **phase 4 complete.**

### phase 5 ... reference forms + multi-voice

- [ ] 5.1 conlang: the constrained coiner + lexicon record-leaf render + grammar reference + phonology registry in `work.settings`.
- [ ] 5.2 encyclopaedia: infobox record + `[[xref]]` inline node + citations -> footnotes/sidenotes + A-Z index.
- [ ] 5.3 `v0_11_0_np_voices.sql` + `getWriterVoice` -> `resolveVoice`, delta-voices, per-span tagging, drift-gated generation. + playwright.

### phase 6 ... the typeset flagship + cleanup

- [ ] 6.1 the `typeset` stage on `pagedjs` + `puppeteer` (+`@sparticuz/chromium`); front/back matter + running headers + the typography pass (`retext-smartypants` no-em-dash + `hyphen`).
- [ ] 6.2 optional `np_exports`; finish the scheduled-publish worker (pg-boss + `scheduled_publish_at` + the partial index exist, no worker runs); Scrivener import (`fast-xml-parser` + `rtf-parser`), isolated, last. + playwright.

## notes / decisions

- the data model is additive + nova-scoped; applying `v0_7_0` touches only `np_*` tables, zero risk to the other products on the shared substrate. the standalone exit ramp (`pg_dump --table='np_*'`) stays intact.
- every increment leaves nova fully working; `/library` + `/editor` + `/p/[slug]` are never broken.
